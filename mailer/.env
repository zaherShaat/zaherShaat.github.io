# ── SMTP Configuration ──
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# ── Gmail Credentials ──
# Use a Gmail App Password (not your regular password)
# Generate one at: https://myaccount.google.com/apppasswords
EMAIL_USER=zaher970597826287@gmail.com
EMAIL_PASS=gblifdbgbevxpwnv

# ── TLS Settings ──
# Set to "false" only if you have certificate issues (not recommended)
TLS_REJECT_UNAUTHORIZED=true

# ── Server Port ──
# Render will override this automatically with its own port
PORT=3000
